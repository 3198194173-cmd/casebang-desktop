# 原有系列补充新产品编码：步骤 2 与出镜壳/出片壳输出修复说明

## 文档目的

本说明交给后续开发账号直接实施。它只描述需要完成的业务和界面改动，不代表本次已经修改了业务代码。

本次范围包含：

- 业务流程“原有系列补充新产品编码”的步骤 2：历史图案对比、命名选择、AI 图案/材质识别、排组区域滚动和页面布局。
- 表格编码及“原有系列补充新产品编码”共用的新建产品表生成规则。
- 出镜壳、出片壳的黑框/银框图片表、条码表、价格和机型编辑规则。

不在本次范围内：

- “原有产品补充新机型”流程的业务规则重做。
- 对已经导出的历史文件进行回写或迁移。

## 已核对的参考文件

参考文件为“表格文件/Rilakkuma Series#J4系列-轻松熊出镜壳.xlsx”。后续实现和验收必须以它的实际结构为准，而不是按目前程序的十列表结构猜测。

该文件包含两个工作表：

- 图片：19 行、11 个业务列（A 到 K）。
- 条码：154 行、9 列（A 到 I）。

### 图片表的正确列结构

- A 列：条码名。
- B 列：图片。全系列主图在 B1，单品图片在各产品行的 B 列。
- C 列：框型标记列。表头可以为空，但该列必须存在并保留样式、宽度和位置。
- D 列：颜色。
- E 到 K 列依次为系列名称、系列名称（大写）、系列编码、产品编码、图片对应名称、图片对应名称（大写）、图档名。

参考文件的第 2 至 5 行是普通款：B 列有图片，C 列为空，表示默认黑框但不显示“黑框”文字。第 6 至 9 行是银框款：B 列再次复制同一批图片，C 列写“银框”。两种框型的产品编码相同。

因此，现有生成器把“颜色”直接放到第三列，导致图片表缺少 C 列并把后续列整体左移，是本次必须修复的结构性问题。不能通过给当前 C 列补颜色来绕过，必须恢复真实的 A–K 列布局。

### 条码表的正确分组含义

参考文件中：

- A 到 I 列为 类目、69码、物料编码（工厂）、物料名称、建议零售价、海外零售价、材质、备注（IP）、名称对应。
- 普通款记录在前，银框记录另起一个空行后的第二组。
- 普通款物料名称不追加“（黑框）”。
- 银框物料名称追加“（银框）”。
- 银框与普通款复用同一产品编码，但作为独立条码记录输出。
- 参考文件的普通款和银框款价格不同。例如普通款常见为 149 / 31.99，银框常见为 169 / 36.99。
- 参考文件中银框并没有机械地覆盖所有机型，说明“是否生成银框”和价格都不能写死为全机型统一规则。

## 一、步骤 2 的目标交互

步骤 2 的目的只有两个：

1. 把新导入的切片与所选历史系列进行可靠对比。
2. 让操作员在当前切片旁边快速看到历史图片并选择已有命名。

页面不能同时长期展示同一批历史图片两次，也不能让历史图片区和新切片编辑区相隔很远。

### 1. 历史系列选择区

保留步骤 2 顶部的“搜索系列名称/代码、A 条码参考中的系列、国内命名表中的位置”三项选择，但改为紧凑的确认区。

- 在下拉列表中勾选一个历史系列和对应国内命名表位置时，临时显示该系列的历史图片预览，供用户确认选对系列。
- 临时预览只服务于“选择前确认”，不作为页面常驻的历史图案栏。
- 增加明确的“确认使用此历史系列”动作。确认后，顶部只保留一条紧凑状态摘要，例如系列代码、英文/中文系列名、国内命名表工作表和历史图片数量。
- 确认后立即收起顶部历史图片预览。不要再在顶部保留横向历史图案排列。
- 不再提供与下方功能重复的“收起历史图案”常驻区域。

确认动作完成后，程序自动开始一次 AI 识别：

- 对每个新增切片比对所选历史系列的图案。
- 同时识别新增切片的材质。
- 用户不需要先点击一个很大的“AI 识别一致图案与材质”按钮。
- 识别进度和结果以紧凑状态显示，不输出逐条长日志，不把新图片向下推得很远。

AI 自动识别的结果是建议，不得静默覆盖人工已经填写的图案名称、产品类别、材质或价格。

- 名称为空时，可显示“AI 推荐名称，待确认”。
- 名称已由操作员输入或从历史图案选择器选定时，只显示比对结果，不覆盖。
- 材质为空或仍为自动默认值时，可回填 AI 结果；人工修改后的材质必须保留。
- 每个切片保留一个轻量状态：已匹配、未匹配、识别失败或待重新识别。必要时提供单个切片重新识别，避免全量重复调用。

### 2. 历史命名选择的唯一入口

历史图片和历史命名的正式选择入口放在“当前区域设置”的“图案对应命名”输入框正下方。

- 默认折叠，仅显示“从 N 个历史图案中选择名称”。
- 点击后展开带搜索的图片卡片列表。每张卡片必须同时显示历史图片和英文图案名称。
- 图片卡片应紧邻当前切片设置，不应出现在步骤 2 顶部，也不应与新切片区域隔开一整屏。
- 选中一张历史图案后，写入当前区域或当前图案组的名称规则仍按现有业务逻辑处理；随后自动关闭图片列表并清空搜索词。
- 选中后不再持续展示整组历史图片。
- 人工输入名称、AI 命名建议、历史名称选择均应使用同一名称校验和重复检查逻辑。

历史名称选择器是人工确认工具，不是新的产品编码复用工具。同图案跨产品类别时，名称可以复用，但产品类别和产品编码仍必须独立新增。

### 3. AI 对比结果的简化

删除或大幅压缩现有“AI 比对历史图案”下方的冗长运行日志。

- 只保留总体进度、成功/失败数量和每个当前切片的一行结果。
- 已匹配时显示推荐历史名称、匹配状态和材质。
- 未匹配时显示“未找到可靠历史图案，可继续 AI 命名或手动命名”。
- 需要“采用”时，让按钮位于该切片结果附近；不要再生成一个大面积结果面板。
- 历史图片很多时，AI 对比应有数量上限、取消能力和清晰提示；人工历史名称选择器必须始终可用。

## 二、步骤 2 的布局和滚动修复

### 1. 页面布局原则

步骤 2 应由三个紧凑层级组成：

1. 历史系列选择与确认摘要。
2. 新导入图片的“图片切片与排组”编辑工作区。
3. 紧邻切片的“当前区域设置”面板。

不要新增“本次新增图案显示排列”这一块。新导入的图片只在图片切片与排组工作区中显示。

历史系列确认前的图片预览可以显示在选择区内；确认后隐藏。历史名称图片只在右侧选择器展开时出现。

### 2. 左右编辑区

- 左侧只负责新导入图片的裁图和图案排组。
- 右侧只负责当前选中区域的名称、历史名称选择、类别、价格/材质等设置。
- 两列顶部对齐，间距紧凑。右侧不能像被隔离在另一个页面。
- 不要用右侧内容的高度强行拉高左侧，使左侧出现大面积空白。
- 当前区域切换后，右侧回到顶部；左侧保持用户正在查看的位置。
- 历史名称选择器应靠近名称输入框，优先于不常用的候选名称详情和其他说明。

### 3. 鼠标滚动必须可用

当前图案排组模式的鼠标滚轮无法可靠滚动，属于功能缺陷，必须修复。

- 页面主滚动由外层页面承担。
- 图片切片与排组的长内容区域必须有明确的可滚动容器和可见滚动条。
- 滚轮在裁图模式、图案排组模式、历史名称选择器和右侧设置面板中都应自然滚动相应最近的容器。
- 图案拖动、裁图框拖动只在实际拖动时拦截指针事件，不能阻断普通滚轮事件。
- 不要在整个左侧工作区使用 touch-action: none、overflow: hidden 或全屏固定高度来吞掉滚动。
- 内层滚动到顶部或底部后，滚轮应继续让外层页面滚动，避免用户被困在一个无法离开的面板中。
- 下拉图片列表可单独限高并内部滚动，但其父容器不能把列表裁掉。

建议移除比较模式下依赖视口高度的强制固定高度，并重新检查所有 overflow、overscroll-behavior、pointer 事件和拖拽事件。验收时必须使用真实鼠标滚轮测试，而不是只验证滚动条存在。

## 三、出镜壳 / 出片壳的框型与新建产品表规则

以下规则同时适用于“表格编码”和“原有系列补充新产品编码”生成的新建产品表。

### 1. 是否生成双框型

- 产品类别为“出镜壳”或“出片壳”时，生成普通款和银框款两套记录。
- 普通款代表黑框，但不在图片表 C 列和条码物料名称中写“黑框”或“（黑框）”。
- 银框款在图片表 C 列写“银框”，在条码物料名称的机型后追加“（银框）”。
- 其他产品类别，例如磁吸背盖、奇趣壳、支架、充电宝等，默认只生成一套记录，不因本规则自动追加银框。
- 是否为出镜壳/出片壳应以当前区域已确认的产品类别为准，不以名称中偶然出现的“壳”字猜测。

### 2. 图片表的生成规则

对于每一个出镜壳或出片壳切片：

1. 生成普通款图片行：B 列写图片，C 列留空。
2. 再生成银框图片行：B 列复制同一张图片，C 列写“银框”。
3. 两行的条码名、系列信息、产品编码、图案名称、图档名保持同一产品逻辑；框型仅由 C 列区分。

输出顺序应稳定：

- 保持产品类别和原始切片顺序。
- 同一类别先输出普通款，再输出该类别的银框款。
- 非双框型类别只输出一组，不插入伪银框行。

必须恢复图片表 A–K 列，并至少满足以下位置：

- A 条码名。
- B 图片。
- C 框型标记。
- D 颜色。
- E–K 保持参考文件中的字段顺序。

图片表 C 列的列宽、格式、空白普通款单元格和银框文字都应与参考文件一致。不能让 D 列颜色左移到 C 列，也不能因为 C 列为空而从导出结构中省略。

### 3. 条码表的生成规则

条码表按“产品类别 → 机型 → 图案”的稳定顺序生成。

- 普通款物料名称：产品条码名加机型，不追加框型文字。
- 银框物料名称：产品条码名加机型，再追加“（银框）”。
- 同一图案的普通款和银框款复用同一个产品编码；银框不是第二个产品编码分配任务。
- 对每一个允许银框的机型，生成一条额外银框条码记录。
- 普通款和银框款之间以空白分组行或与参考模板相同的分组方式分开，不能无提示地交叉混排。

## 四、机型编辑、框型启用和价格设置应放在步骤 3

用户已明确指出：区域设置里的输入框已经太多。机型差异、框型和价格不应继续堆在步骤 2，而应在“编码与机型”步骤统一维护。

### 1. 机型名称可编辑

步骤 3 的机型清单不能只是一组固定复选框。每个已选机型都需要：

- 是否启用。
- 可编辑的机型名称。
- 品牌大类：苹果、三星、华为、其他。
- 是否生成银框款，仅在本次存在出镜壳/出片壳时显示。
- 必要时的单机型价格覆盖。

机型名称就是最终“添加条码”时写进物料名称的名称。用户修改后，生成的条码表必须使用修改后的文本，不得仍使用旧名称或参考列表名称。

为避免修改名称后价格和框型设置丢失，内部状态应使用稳定的机型记录 ID；显示名称只是可编辑字段。需要阻止空名称和重复名称。

### 2. 价格矩阵

对于本次出现的每个“出镜壳”或“出片壳”类别，步骤 3 显示紧凑的价格矩阵，而不是在每个切片旁重复价格输入框。

矩阵至少要支持：

- 普通款的国内建议零售价和海外零售价。
- 银框款的国内建议零售价和海外零售价。
- 苹果、三星、华为、其他四个品牌大类的默认价格。
- 单机型价格覆盖。只有用户填写覆盖值时才替代品牌大类默认价格。
- 单机型是否启用银框。这样可表达参考文件中“普通款覆盖较多机型、银框仅覆盖部分机型”的实际情况。

价格优先级必须固定为：

1. 单机型 + 框型价格覆盖。
2. 品牌大类 + 框型价格。
3. 当前产品类别的框型默认价格。
4. 现有业务规则中的基础价格预设。

若最终没有得到有效国内或海外价格，步骤 4 质检必须明确阻止导出并定位到对应类别、框型和机型，不能默默使用错误价格。

出镜壳/出片壳之外的产品继续沿用现有单套价格逻辑；不要因为新增价格矩阵而让其他产品多出银框设置。

### 3. 默认值

参考文件可作为默认建议，而不是固定业务常量：

- 普通款常见默认：149 / 31.99。
- 银框款常见默认：169 / 36.99。

不同品牌大类和不同机型可以改成不同价格。因此界面必须呈现“默认值来自规则，可编辑”，而不是把上述数字写死到条码生成器中。

## 五、现有实现中的已知根因和改动位置

以下是当前代码结构中需要重点修改的位置，方便后续账号快速定位。

- 生成图片表的构建函数目前仅定义十列，顺序从“条码名、图片、颜色”开始，缺少 C 列框型。这是图片表列错位的直接根因。需要扩展为 A–K 结构，并建立普通/银框行。
- 条码表构建函数目前每个产品切片和机型只生成一条记录，未建模框型变体。需要在这里按机型启用状态展开普通/银框记录，但不能为银框重新申请产品编码。
- 现有机型状态是名称字符串数组加品牌映射，无法保存“可编辑名称、框型启用、品牌价格、单机型覆盖”。需要扩展任务草稿、共享契约、校验 schema 和草稿迁移。
- 步骤 3 的编码界面是机型复选框和新增机型输入。需要在此处加入可编辑机型行和价格矩阵，不能把这些设置塞回步骤 2。
- 步骤 2 的历史系列选择组件当前会长期渲染历史图片条，并单独渲染 AI 比对面板。需要改为“选择时临时预览、确认后收起并自动比对”。
- 当前历史名称选择器已经接近目标，但它必须成为唯一的常用历史图片入口，选中后自动关闭，并且与名称输入框紧邻。
- 比较模式的裁图工作区当前使用固定视口高度和多层滚动/裁剪设置，造成图案排组滚轮体验异常和留白。需要重新划分页面、工作区和下拉列表的滚动责任。

优先检查的源文件包括：

- src/renderer/src/features/tasks/ExistingSeriesPicker.tsx
- src/renderer/src/features/tasks/PatternComparisonPanel.tsx
- src/renderer/src/features/tasks/HistoricalNamePicker.tsx
- src/renderer/src/features/tasks/ImageCropWorkspace.tsx
- src/renderer/src/features/tasks/EncodingPreviewWorkspace.tsx
- src/renderer/src/features/tasks/NewTaskPage.tsx
- src/renderer/src/features/tasks/generation-workspace-builder.ts
- src/renderer/src/features/tasks/ExportWorkspace.tsx
- src/shared/contracts.ts
- src/shared/image-contracts.ts
- src/shared/schemas.ts
- src/shared/generation-contracts.ts
- src/shared/product-business-rules.ts
- src/main/modules/tasks/format-preserving-generation-exporter.ts
- src/renderer/src/styles/global.css

涉及输出结构时，必须同时检查预览数据、实际 Excel 写入、格式保留导出和质检，不可只修改前端预览。

## 六、草稿兼容和数据安全

- 新增的机型记录、框型启用状态和价格矩阵必须写入工作草稿，切换业务流程后不能丢失。
- 旧草稿打开时必须能升级：保留原有已选机型和品牌；普通款继续可生成；仅对出镜壳/出片壳补充可编辑的银框默认设置。
- 手动修改过的名称、材质、机型名称、价格或框型启用状态优先级最高，AI 和默认值都不能覆盖。
- 历史基础资料、国内命名表和 A 条码参考的写入坐标规则不得因为新增图片表双框型而改变。
- 生成前必须明确显示：将生成多少普通款图片行、银框图片行、普通条码行和银框条码行。

## 七、验收清单

### 步骤 2

- 选择历史系列时可以临时看见历史图片；确认后顶部图片预览消失，只保留紧凑摘要。
- 确认历史系列后自动启动一次图案与材质识别。
- 历史名称图片只在当前区域设置的下拉选择器展开时显示。
- 选择历史名称后，下拉自动关闭，图片列表不常驻。
- 切片区、排组区、右侧设置区和历史名称下拉均可用鼠标滚轮自然滚动。
- 拖动裁图框和拖动图案排组不影响普通滚轮。
- 页面没有重复的历史图案展示、冗长 AI 日志或大面积无意义留白。

### 出镜壳 / 出片壳

- 新建产品表“图片”工作表有 A–K 列，C 列存在且没有导致后续列左移。
- 普通款图片行的 C 列为空，B 列有图片。
- 银框图片行的 C 列为“银框”，B 列复制相同图片。
- 同图案普通/银框产品编码相同，不会多占用一个编码池号码。
- 普通条码名称不出现“黑框”；银框条码名称出现“（银框）”。
- 普通和银框价格可不同，且可按苹果、三星、华为、其他和单机型配置。
- 对不启用银框的机型，不生成银框条码行。
- 磁吸背盖、奇趣壳、支架等非目标类别不会被自动复制成银框。

### 机型和价格

- 操作员可以修改已选机型名称，导出条码物料名称立即使用修改后的名称。
- 机型名称为空、重复、未配置必要价格时，步骤 4 能指出具体项目并阻止导出。
- 修改一个机型的价格或银框启用状态，不影响其他机型。
- 修改一个品牌大类默认价会更新未被单机型覆盖的对应记录。

### 回归测试

至少新增自动化测试覆盖：

- 图片表从十列恢复为 A–K，C 列框型和 D 列颜色位置正确。
- 出镜壳、出片壳的普通/银框图片和条码成对输出。
- 非出镜壳、非出片壳不生成银框。
- 同图案双框型复用产品编码。
- 普通款和银框款分别使用各自价格。
- 品牌默认价与单机型覆盖的优先级。
- 编辑机型名称后，条码物料名称使用新名称。
- 保存后重新打开草稿，框型和价格设置仍存在。
- 用参考文件“Rilakkuma Series#J4系列-轻松熊出镜壳.xlsx”进行结构对照：图片列、C 列框型、B 列图片重复、条码分组与银框后缀均正确。

完成后应进行一次实际导出，在 Excel/WPS 中核验图片、列宽、图片锚点、空白分组行、价格显示和长机型名称，不能只依赖网页预览。
